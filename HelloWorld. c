#include <iostream> // includes standard input/output routines
// < > it refers to a part of the C++ library called a standard header
int main() // function of name ’main’ and type int, so it (ideally) requires to return an integer
// () indicates no parameters
{ // left brace, beginning of statements
std::cout << "Hello World" << std::endl; //std:: is part of a namespace
//std:endl is a manipulator
// << is standard library’s output operator
// it writes Hello World on the standard output
return 0; // The value that is returned must have a type that is appropriate for the type that the function says it will return
} // right brace, end of statements
// output: Hello World
